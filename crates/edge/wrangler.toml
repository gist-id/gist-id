name = "gist-id-edge"
main = "build/worker/shim.mjs"
compatibility_date = "2025-01-01"

[build]
command = "cargo install -q worker-build && worker-build --release"

# Production deployment: gist.id apex.
# Add the route via the Cloudflare dashboard after first deploy, or uncomment
# below once the zone is associated with the account.
# routes = [
#   { pattern = "gist.id/*", zone_name = "gist.id" }
# ]
